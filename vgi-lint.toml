# No rule waivers, no ignore list, no execution override. The worker's metadata passes
# `--fail-on info` on its own. `fail_on = "info"` is intentionally STRICTER than the default
# (which fails only on errors); `select = ["ALL"]` runs every rule.
[tool.vgi-lint-check]
select = ["ALL"]
fail_on = "info"
